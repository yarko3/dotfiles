*.un~
*.hi
*.log
*.o
*.pyc
nodes
tags

/* only keep plugin manager */
tmux/plugins
!tmux/plugins/tpm

vim/autoload/plug.vim.old

/* spell files are pretty big */
vim/spell/en.utf-8.*

/* compiled spell files */
vim/spell/*.spl

/* don't include nvim symlink in repo; it is handled by dotbot */
vim/init.vim

/* xmonad temporaries */
xmonad-x86*
xmonad.errors
xmonad.state
xmonad/lib
xmonad/xmonad

/* files managed outside of this repository */
vim/lua/local_lsp_config.lua
